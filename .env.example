
# Variáveis de Ambiente - Exemplo
# 
# NOTA: Este arquivo é OPCIONAL para deployment local com Docker.
# Os valores padrão já estão configurados no docker-compose.yml.
# Copie este arquivo para .env apenas se desejar customizar as configurações.

# Configuração da API
SECRET_KEY_BASE=altere_para_uma_string_aleatoria_segura

# Configuração de Portas (opcional, valores padrão mostrados)
# API_PORT=3000
# FRONTEND_PORT=8080

# URL da API para o frontend (opcional, auto-configurado no docker-compose)
# API_URL=http://api:3000
