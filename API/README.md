# README

## Database Setup

To populate the database during initial setup, run:

```bash
bin/rails db:setup
```

This command will create the database, load the schema, and seed it with initial data from `db/seeds/pedidos.json`.